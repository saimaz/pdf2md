[project]
name = "pdf2md"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "markitdown[pdf]",
    "prometheus-client",
]
